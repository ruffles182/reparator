<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="reparator.list">
      <field name="name">reparator list</field>
      <field name="model">reparator.reparator</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="reparator.action_window">
      <field name="name">reparator window</field>
      <field name="res_model">reparator.reparator</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="reparator.action_server">
      <field name="name">reparator server</field>
      <field name="model_id" ref="model_reparator_reparator"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="reparator" id="reparator.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="reparator.menu_1" parent="reparator.menu_root"/>
    <menuitem name="Menu 2" id="reparator.menu_2" parent="reparator.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="reparator.menu_1_list" parent="reparator.menu_1"
              action="reparator.action_window"/>
    <menuitem name="Server to list" id="reparator" parent="reparator.menu_2"
              action="reparator.action_server"/>
    -->
  </data>
</odoo>